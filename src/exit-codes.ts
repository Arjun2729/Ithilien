/** Process completed successfully. */
export const EXIT_SUCCESS = 0;

/** Verification or integrity check failed (chain broken, signature invalid, etc.). */
export const EXIT_VERIFICATION_FAILED = 1;

/** Invalid input (file not found, missing session, malformed bundle). */
export const EXIT_INVALID_INPUT = 2;
